= form_for @user do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |message|
          %li= message

  %h1 回家11111的卡拉和进口寒假快乐寒假快乐
  .field
    = f.label :name
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :email
    = f.text_field :email
  .field
    = f.label :email
    = f.text_field :email
  .actions
    = f.submit 'Save'
